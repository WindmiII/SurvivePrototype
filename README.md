# 🎮 SurvivePrototype

โปรเจกต์เกมยิงเอาตัวรอด 2D

## 🕹️ วิธีเล่น (How to Play)
* **W, A, S, D**: เคลื่อนที่ตัวละคร
* **Mouse Click**: ยิงกระสุนไปตามทิศทางเมาส์
* **Goal**: กำจัดศัตรูเพื่อทำคะแนนให้มากที่สุด และรักษาชีวิตให้รอด (มี 3 หัวใจ)

---

## 🛠️ การปรับปรุงโครงสร้างโค้ด (Refactoring with Design Patterns)

โปรเจกต์นี้เริ่มต้นจากการเขียนโค้ดแบบดั้งเดิมที่ทุกคลาสผูกติดกัน จากนั้นจึงทำการ Refactor โดยใช้ Design Patterns ดังนี้:

### 1. Factory Pattern (สำหรับระบบสร้างศัตรู)
* **ปัญหาเดิม:** `GameManager` ทำหน้าที่เยอะเกินไป ทั้งคุม Loop เกม และรับผิดชอบ Logic การ `Instantiate` ศัตรู ทำให้การแก้ไขประเภทศัตรูทำได้ยาก
* **การแก้ไข:** สร้างคลาส `EnemyFactory` แยกออกมาเพื่อรับผิดชอบการสร้าง Object และคำนวณตำแหน่งเกิดโดยเฉพาะ
* **ผลลัพธ์:** `GameManager` ไม่จำเป็นต้องรู้ว่าศัตรูถูกสร้างอย่างไร ลดความซับซ้อนของโค้ดหลัก

### 2. Observer Pattern (สำหรับระบบ UI และ Events)
* **ปัญหาเดิม:** `PlayerController` และ `Bullet` ต้องอ้างอิงถึง UI โดยตรง ทำให้เมื่อมีการแก้ UI หรือลบ Object จะเกิด Error ทันที
* **การแก้ไข:** ใช้ระบบ **Event** `Action` ผ่านคลาส `GameEvents` เพื่อส่งสัญญาณเมื่อเกิดเหตุการณ์ต่างๆ โดยมี `UIManager` คอยดักฟัง (Subscribe)
* **ผลลัพธ์:** Logic ของเกมแยกออกจาก UI โดยสิ้นเชิง ตัวละครแค่บอกว่าโดนตี แต่ไม่ต้องสนใจว่าหลอดเลือดจะลดหรือไม่ ทำให้โค้ดสะอาดและดูแลรักษาง่าย
